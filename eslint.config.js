import js from '@eslint/js';
import globals from 'globals';
import tseslint from 'typescript-eslint';

export default tseslint.config(
  {
    ignores: [
      'node_modules/**',
      '**/dist/**',
      'coverage/**',
      'test-results/**',
      'playwright-report/**',
      // Generated by scripts/build-browser-bootstrap.mjs / copy-core-for-extension.mjs — not source.
      'apps/extension/browser-bootstrap.bundle.js',
      'apps/extension/core/**',
    ],
  },
  js.configs.recommended,
  ...tseslint.configs.recommended,
  {
    // packages/**/*.ts run in both Node (Playwright/exporter/CLI) and the browser
    // (browser-bootstrap.ts, injected into a page); tests run under Node+jsdom.
    files: ['packages/**/*.ts', 'apps/**/*.ts', 'tests/**/*.ts', '*.ts'],
    languageOptions: {
      globals: { ...globals.node, ...globals.browser },
    },
    rules: {
      // Recorder events intentionally carry loosely-typed payloads (RecordingEvent.data,
      // DOM snapshot trees) that get narrowed with casts at the edges.
      '@typescript-eslint/no-explicit-any': 'off',
      '@typescript-eslint/no-unused-vars': ['warn', { argsIgnorePattern: '^_' }],
    },
  },
  {
    // The Chrome extension and demo/devtools UIs are plain JS, injected into or running
    // directly in a browser/extension context with no build step.
    files: ['apps/extension/**/*.js', 'apps/devtools/**/*.js', 'apps/demo/public/**/*.js'],
    languageOptions: {
      globals: { ...globals.browser, chrome: 'readonly' },
    },
    rules: {
      'no-unused-vars': ['warn', { argsIgnorePattern: '^_' }],
      '@typescript-eslint/no-unused-vars': 'off',
    },
  },
  {
    files: ['scripts/**/*.mjs'],
    languageOptions: {
      globals: globals.node,
    },
  },
);
